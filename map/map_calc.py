import tensorflow as tf

def map(output,labels):
    # [b,w,h,3,6]
    x1 = output[...,0]
    x11 = labels = [...,]
    intersection = tf.minimum(output[...,0],labels[...,0])
    tf.minimum()
    map = ''
    return map

